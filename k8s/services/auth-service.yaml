apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: default
spec:
  internalTrafficPolicy: Cluster
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: auth
  sessionAffinity: None
  type: ClusterIP
