apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: default
spec:
  internalTrafficPolicy: Cluster
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8888
  selector:
    app: api-gateway
  sessionAffinity: None
  type: ClusterIP
